{
  "name": "mplad-blockchain-integrity",
  "version": "1.0.0",
  "description": "Hardhat Smart Contracts for MPLAD Audit Registry",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.18"
  }
}
